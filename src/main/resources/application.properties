# configs
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER
spring.http.multipart.enabled=true

# database
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres?currentSchema=test_api
spring.datasource.driver-class-name=org.postgresql.Driver
spring.sql.init.platform=postgres
spring.datasource.username=superuser
spring.datasource.password=3226018
spring.jpa.hibernate.ddlAuto=validate

#Swagger
#For use swagger use the url: localhost:8080/swagger-ui/index.html

#Liquibase
spring.liquibase.enabled=true